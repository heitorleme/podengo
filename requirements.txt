# --- Streamlit + UI ---
streamlit>=1.36.0

# --- Manipulação de dados ---
pandas>=2.1.0
openpyxl>=3.1.0     # engine para exportar Excel

# --- Logs / utilidades ---
loguru>=0.7.0
tqdm>=4.66.0
tenacity>=8.2.3

# --- MongoDB ---
pymongo>=4.6.0

# --- API Clients ---
openai>=1.30.0
tiktoken>=0.7.0
apify-client>=1.6.0

# --- Pydantic + settings ---
pydantic>=2.7.0
pydantic-settings>=2.1.0

# --- Scraping / social ---
instaloader>=4.10.0
requests>=2.31.0

# --- Media / processamento ---
opencv-python-headless>=4.9.0.80  # versão headless (dispensa GUI)
filetype>=1.2.0

# --- Áudio / vídeo ---
ffmpeg-python>=0.2.0                                # wrapper opcional p/ ffmpeg (não estritamente obrigatório)

# --- Outras libs úteis ---
logging
python-dotenv>=1.0.0     # se quiser ler .env (pydantic já ajuda, mas útil)
psutil
